services:
  proxy:
    build: ../proxy
    container_name: carparts-proxy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./certs:/etc/nginx/certs
