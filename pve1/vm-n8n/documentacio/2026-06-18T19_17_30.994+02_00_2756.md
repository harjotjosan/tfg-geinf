# Informe Tècnic d'Incidència d'Infraestructura

## 1. Resum Executiu
- **ID execució:** 2756
- **Data/Hora Detecció:** 2026-06-18T17:12:59Z
- **Data/Hora Resolució:** 2026-06-18T17:20:00Z (aprox.)
- **Node Afectat:** Proxy VM (10.0.27.228)
- **Servei / Contenidor:** carparts-proxy (Nginx)
- **Severitat de l'Alerta:** Crítica (Interrupció total del servei web)
- **Estat Final del Servei:** OPERATIONAL

---

## 2. Símptomes Detectats
L'incidència s'ha detectat mitjançant la verificació de l'estat del servei, observant que el contenidor de proxy no responia a les peticions HTTP.

Evidència de l'error capturada durant el diagnòstic:
`[emerg] 1#1: invalid parameter "server_name" in /etc/nginx/nginx.conf:11`

---

## 3. Diagnòstic de la Infraestructura
Després de rebre l'alerta, s'han executat comandes de diagnòstic no destructives directament sobre el node afectat:

- **Estat del contenidor Docker (via SSH):**
  La sortida de la comanda d'inspecció és: `"State": {"Status": "restarting", "Restarting": true, "ExitCode": 1}, "RestartCount": 14`.
  *Anàlisi:* El contenidor es trobava en un bucle de reinici constant (crash loop). L'estat "restarting" i el recompte de 14 reinicis confirmen que el servei no podia iniciar-se correctament a causa d'un error crític de configuració interna.

---

## 4. Accions Executades
Descriu de manera cronològica i concisa les accions realitzades:
1. **Verificació d'estat:** Comprovació de l'accessibilitat del servei i detecció de la caiguda del Proxy.
2. **Extracció de configuració:** Utilització de `docker cp` per extreure el fitxer `/etc/nginx/nginx.conf` del contenidor al host per a la seva anàlisi.
3. **Anàlisi de sintaxi:** Identificació d'un error de sintaxi a la línia 11: falta de punt i coma (`;`) després de la directiva `listen 80`.
4. **Restauració de fitxer:** Copia del fitxer de configuració correcte des del host (`/root/tfg-management/pve1/proxy/nginx.conf`) cap a l'interior del contenidor.
5. **Reinici del servei:** Execució de `docker restart carparts-proxy` per aplicar els canvis.

---

## 5. Resultat Final
A data de `2026-06-18T17:20:00Z`, la verificació d'estat via `docker ps` retorna que el contenidor està `Up 12 seconds` amb el mapeig de ports `0.0.0.0:80->80/tcp` actiu. El servei se situa operatiu i accessible, confirmant que la correcció de la sintaxi de Nginx ha resolt l'incidència.

---

## 6. Errors Pendents o Riscos
- **Anàlisi de persistència:** S'ha detectat que el contenidor no utilitza un volum muntat per al fitxer de configuració de Nginx, sinó que el fitxer resideix internament. Això implica que qualsevol modificació manual interna es perdrà en cas de recreació del contenidor, o que l'imatge base pot contenir l'error de sintaxi.

---

## 7. Recomanacions Futures
- **Implementació de Volums:** Configurar el fitxer `docker-compose.yml` per muntar el fitxer `/root/tfg-management/pve1/proxy/nginx.conf` com a volum en `/etc/nginx/nginx.conf`. Això garantirà la persistència de la configuració i facilitarà les actualitzacions sense necessitat de `docker cp`.
- **Validació Prèvia:** Implementar un pas de validació de sintaxi (`nginx -t`) en el pipeline de desplegament abans de reiniciar el servei en producció.