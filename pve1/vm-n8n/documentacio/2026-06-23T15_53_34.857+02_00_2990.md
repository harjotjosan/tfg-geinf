# Informe Tècnic d'Incidència d'Infraestructura

## 1. Resum Executiu
- **ID execució:** 2990
- **Data/Hora Detecció:** 2026-06-23 15:41:54 CEST
- **Data/Hora Resolució:** 2026-06-23 16:10:00 CEST
- **Node Afectat:** 10.0.27.228 (Proxy VM)
- **Servei / Contenidor:** carparts-proxy (Nginx)
- **Severitat de l'Alerta:** Crítica
- **Estat Final del Servei:** OPERATIONAL

---

## 2. Símptomes Detectats
L'incidència s'ha detectat mitjançant la verificació de la validesa del certificat SSL del domini `carparts.example.com`.

Evidència de la traça de validació capturada via `openssl`:
`notBefore=Jun 22 10:16:04 2025 GMT`
`notAfter=Jun 22 10:16:04 2026 GMT`

L'anàlisi contra la data del sistema (`Tue Jun 23 15:41:54 CEST 2026`) confirma que el certificat ha caducat el dia anterior.

---

## 3. Diagnòstic de la Infraestructura
S'han executat comandes de diagnòstic no destructives sobre el node 10.0.27.228:

- **Estat del contenidor Docker (via SSH):**
  La sortida de la comanda d'inspecció de fitxers és: 
  `-rw-r--r-- 1 root root 1086 Jun 23 10:16 expired.crt`
  `-rw-r--r-- 1 root root 1679 Jun 23 10:16 expired.key`
  `-rw-r--r-- 1 root root 1086 Jun 23 10:16 valid.crt.bak`
  `-rw-r--r-- 1 root root 1675 Jun 23 10:16 valid.key.bak`
  
  *Anàlisi:* El contenidor `carparts-proxy` estava operational, però la configuració de Nginx apuntava a fitxers de certificats caducats (`expired.crt`/`expired.key`). S'ha identificat la presència de còpies de seguretat vàlides en el mateix directori.

---

## 4. Accions Executades
Descripció cronològica de les accions realitzades:
1. **Verificació Temporal:** Execució de `date` per establir el marc temporal del sistema.
2. **Auditoria SSL:** Execució de `openssl s_client` per confirmar la data de caducitat del certificat actiu.
3. **Inspecció de Fitxers:** Llistat del directori `/etc/nginx/certs/` per identificar alternatives de certificats.
4. **Validació de Backup:** Verificació de la data de validitat del fitxer `valid.crt.bak` (Vàlid fins al 2027-06-23).
5. **Restauració de Certificats:** Substitució dels fitxers `expired.crt` i `expired.key` pels fitxers `valid.crt.bak` i `valid.key.bak`.
6. **Reinici del Servei:** Execució de `docker restart carparts-proxy` per aplicar els canvis de configuració.

---

## 5. Resultat Final
A data de `2026-06-23 16:10:00 CEST`, la prova de connectivitat SSL retorna la següent validesa:
`notBefore=Jun 22 10:16:04 2026 GMT`
`notAfter=Jun 23 10:16:04 2027 GMT`

El servei se situa operatiu i accessible via HTTPS, confirmant la resolució de l'incidència mitjançant la restauració de certificats vàlids.

---

## 6. Errors Pendents o Riscos
- **Anàlisi de persistència:** La resolució s'ha basat en la restauració d'un backup manual. L'absència d'un mecanisme d'autorenovació (com Let's Encrypt/Certbot) implica un risc de recursió de la incidència quan el certificat actual caduqui el juny de 2027.

---

## 7. Recomanacions Futures
- Implementar un sistema de renovació automatitzada de certificats SSL per evitar la dependència de backups manuals.
- Configurar una alerta de monitorització proactiva que notifiqui la caducitat del certificat SSL amb 15 i 30 dies d'antelació.