# Informe Tècnic d'Incidència d'Infraestructura

## 1. Resum Executiu
- **ID execució:** 3174
- **Data/Hora Detecció:** 2026-06-29T16:31:34+02:00
- **Data/Hora Resolució:** 2026-06-29T14:31:34+00:00
- **Node Afectat:** web1 (10.0.27.223)
- **Servei / Contenidor:** carparts-web
- **Severitat de l'Alerta:** critical
- **Estat Final del Servei:** OPERATIONAL

---

## 2. Símptomes Detectats
L'alerta s'ha disparat automàticament mitjançant la monitorització de logs centralitzats a 'rsyslog-detector' al fitxer `/var/log/remote/10.0.27.223/docker-carparts-web.log`.

Evidència de la línia de log capturada:
`2026-06-29T16:31:34+02:00 web1 docker-carparts-web: [SECURITY] SQL Injection attempt detected`

---

## 3. Diagnòstic de la Infraestructura
Després de rebre l'alerta, s'han executat comandes de diagnòstic no destructives directament sobre el node afectat:

- **Estat del contenidor Docker (via SSH):**
  La sortida de la comanda d'inspecció és: `[SECURITY] SQL Injection attempt detected ... [ERROR] Database query execution failed: invalid input syntax for type boolean: ""`
  *Anàlisi:* El contenidor `carparts-web` portava en execució des de les 10:40:09 UTC, comprovant que el procés de Gunicorn s'havia mantingut actiu durant diverses hores abans de la detecció de l'atac. L'incident no va causar la caiguda del contenidor, però va generar errors de sintaxi en les consultes a la base de dades.

---

## 4. Accions Executades
Descriu de manera cronològica i concisa les accions realitzades pel flux automatitzat de n8n:
1. Recepció de la traça del log crític de seguretat (patró `sql_injection`) des del servei de detecció de patrons.
2. Connexió SSH automatitzada al node `web1` per inspeccionar els logs de l'aplicació i verificar l'estat del contenidor Docker.
3. Verificació de la connectivitat de xarxa entre el node Web (10.0.27.223) i el node de base de dades (10.0.27.239) mitjançant `nc`.
4. Modificació de la configuració de l'entorn en el fitxer `docker-compose.yml` per desactivar la vulnerabilitat (`SQL_INJECTION_VULNERABLE: "false"`).
5. Recreació del contenidor `carparts-web` mitjançant `docker compose up -d` per aplicar els nous paràmetres de seguretat.

---

## 5. Resultat Final
A data de 2026-06-29T14:31:34+00:00, les proves de connectivitat web retornen un codi d'estat `200` amb un temps total de resposta validat mitjançant logs de l'aplicació. El servei se situa operatiu i accessible, confirmant que la incidència s'ha resolt mitjançant la mitigació de la vulnerabilitat a nivell de configuració d'entorn.

---

## 6. Errors Pendents o Riscos
- **Anàlisi de persistència:** La mitigació aplicada és a nivell de configuració de l'entorn (`environment variables`). Existeix un risc de seguretat si la variable `SQL_INJECTION_VULNERABLE` es torna a canviar a `true`, ja que la vulnerabilitat de codi (falta d'ús de consultes parametrizades) persisteix en la lògica de l'aplicació Python Flask.

---

## 7. Recomanacions Futures
- Realitzar una revisió urgent del codi font de l'aplicació per substituir l'ús de formatació de cadenes en les consultes SQL per **consultes parametrizades (prepared statements)**.
- Mantenir el monitoratge estricte d'aquest patró de log en el node de seguretat.