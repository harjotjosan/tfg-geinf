# Informe Tècnic d'Incidència d'Infraestructura

## 1. Resum Executiu
- **ID execució:** 2959
- **Data/Hora Detecció:** 2026-06-23 13:31:32 CEST
- **Data/Hora Resolució:** 2026-06-23 13:45:00 CEST (estimat)
- **Node Afectat:** 10.0.27.228 (Proxy VM)
- **Servei / Contenidor:** carparts-proxy (Nginx)
- **Severitat de l'Alerta:** Alta (Certificat SSL caducat)
- **Estat Final del Servei:** OPERATIONAL

---

## 2. Símptomes Detectats
L'alerta s'ha disparat automàticament mitjançant la monitorització de la validesa del certificat SSL a l'endpoint extern.

Evidència de la línia de l'evidència original:
`notAfter=Jun 22 10:16:04 2026 GMT`

---

## 3. Diagnòstic de la Infraestructura
Després de rebre l'alerta, s'han executat comandes de diagnòstic no destructives directament sobre el node afectat:

- **Estat del contenidor Docker (via SSH):**
  La sortida de la comanda d'inspecció és: `{"Status": "running", "Running": true, "StartedAt": "2026-06-23T11:13:06.057057923Z"}`.
  *Anàlisi:* El contenidor `carparts-proxy` estava en execució i operatiu des de l'inici de la sessió, però el servei HTTPS no era accessible de manera segura perquè la configuració de Nginx apuntava a fitxers de certificat (`expired.crt` i `expired.key`) que havien caducat el dia anterior.

---

## 4. Accions Executades
Descriu de manera cronològica i concisa les accions realitzades pel flux automatitzat:
1. Verificació de la data de caducitat del certificat SSL mitjançant `openssl s_client` connectant amb el port 443 del node 10.0.27.228.
2. Inspecció de la configuració interna del contenidor Nginx (`/etc/nginx/nginx.conf`) per identificar les rutes dels certificats actius.
3. Localització i validació de certificats nous i vàlids (`valid.crt` i `valid.key`) en el volum muntat de l'host `/root/tfg-management/pve1/vm-proxy/certs`.
4. Execució de comandes `sed` per substituir les referències dels certificats caducats pels nous i recàrrega del procés Nginx (`nginx -s reload`) dins del contenidor.

---

## 5. Resultat Final
A data de 2026-06-23, les proves de connectivitat web retornen un certificat vàlid amb una data de caducitat de `notAfter=Jun 23 10:16:04 2027 GMT`. El servei se situa operatiu i accessible, confirmant que la incidència s'ha resolt mitjançant l'actualització de la configuració de seguretat.

---

## 6. Errors Pendents o Riscos
- **Anàlisi de persistència:** L'error ha estat causat per una configuració manual de rutes de fitxers específics en lloc de l'ús de certificats gestionats automàticament. Existeix un risc de repetició si no s'implementa un mecanisme de renovació automàtica (ACME/Certbot) que actualitzi els fitxers en el directori de muntatge sense requerir intervenció manual de la configuració de Nginx.

---

## 7. Recomanacions Futures
- Implementar un procés de renovació automàtica de certificats (ex. Certbot) configurat per escriure directament en el directori `/root/tfg-management/pve1/vm-proxy/certs`.
- Modificar la configuració de Nginx per utilitzar noms de fitxers estàtics (ex. `current.crt`) que siguin actualitzats per un script de renovació, evitant així la necessitat de modificar el fitxer `nginx.conf` cada vegada que el certificat expira.