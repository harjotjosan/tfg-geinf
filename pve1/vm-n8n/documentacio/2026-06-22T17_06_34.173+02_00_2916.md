# Informe Tècnic d'Incidència d'Infraestructura

## 1. Resum Executiu
- **ID execució:** 2916
- **Data/Hora Detecció:** 2026-06-22T16:56:37+02:00
- **Data/Hora Resolució:** 2026-06-22T15:00:00 (Validació de connectivitat post-reparació)
- **Node Afectat:** web1 (10.0.27.223)
- **Servei / Contenidor:** carparts-web
- **Severitat de l'Alerta:** high
- **Estat Final del Servei:** OPERATIONAL

---

## 2. Símptomes Detectats
L'alerta s'ha disparat automàticament mitjançant la monitorització de logs centralitzats a 'rsyslog-detector' al fitxer `/var/log/remote/10.0.27.223/docker-carparts-web.log`. 

Evidència de la línia de log capturada:
`2026-06-22T16:56:37+02:00 web1 docker-carparts-web: [2026-06-22 14:56:37 +0000] [1] [CRITICAL] WORKER TIMEOUT (pid:18)`

---

## 3. Diagnòstic de la Infraestructura
Després de rebre l'alerta, s'han executat comandes de diagnòstic no destructives directament sobre el node afectat:

- **Estat del contenidor Docker (via SSH):**
  La sortida de la comanda d'inspecció és: `f365481fdc9d vm-web-web "gunicorn --bind 0.0…" 3 weeks ago Up 4 days 0.0.0.0:8000->8000/tcp, [::]:8000->8000/tcp carparts-web`.
  *Anàlisi:* El contenidor porta 4 dies actiu. Els errors de `WORKER TIMEOUT` i `SIGKILL` indicaven que els processos Gunicorn no podien completar les peticions, derivat de la incapacitat de connectar amb el node de base de dades.

---

## 4. Accions Executades
1. Recepció de la traça del log crític `worker_timeout` des del servei de detecció de patrons.
2. Connexió SSH al node Proxy (10.0.27.228) per verificar l'estat de Nginx, detectant errors `502 Bad Gateway` i `upstream prematurely closed connection`.
3. Connexió SSH al node Web (10.0.27.223) per verificar l'estat del contenidor i la connectivitat de xarxa, detectant un *timeout* en la petició `nc -zv 10.0.27.239 5432`.
4. Connexió SSH al node DB (10.0.27.239) per inspeccionar les regles de firewall, detectant regles `DROP` actives per al port 5432 en les cadenes `INPUT` i `DOCKER-USER`.
5. Eliminació de les regles de bloqueig de `iptables` (`sudo iptables -D INPUT -p tcp -m tcp --dport 5432 -j DROP` i `sudo iptables -D DOCKER-USER -p tcp -m tcp --dport 5432 -j DROP`) per restaurar el trànsit de dades.

---

## 5. Resultat Final
A data de 2026-06-22T15:00:00, les proves de connectivitat de xarxa cap al port 5432 del node de base de dades retornen un èxit de connexió. El servei se situa operatiu i accessible, confirmant que la incidència s'ha resolt mitjançant la correcció de la configuració de seguretat del node de base de dades.

---

## 6. Errors Pendents o Riscos
- **Anàlisi de persistència:** En tractar-se d'un error de timeout d'un worker, hi ha un risc de recursió si l'aplicació pateix pics de càrrega de treball o si existeixen consultes bloquejants o bloquejos mutus (deadlocks) amb el node de base de dades central de la infraestructura.

---

## 7. Recomanacions Futures
- Mantenir el monitoratge estricte d'aquest patró de log. Si la freqüència d'aparició augmenta, es recomana augmentar el valor del paràmetre del temps d'espera (timeout) dels workers dins de la configuració de Gunicorn al desplegament de l'aplicació, o revisar el rendiment de les consultes SQL cap a la base de dades.