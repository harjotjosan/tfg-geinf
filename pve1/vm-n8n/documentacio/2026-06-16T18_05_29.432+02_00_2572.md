# Informe Tècnic d'Incidència d'Infraestructura

## 1. Resum Executiu
- **ID execució:** 2572
- **Data/Hora Detecció:** 2026-06-16T14:12:34+02:00
- **Data/Hora Resolució:** 2026-06-16T16:02:59 GMT
- **Node Afectat:** web1 (10.0.27.223)
- **Servei / Contenidor:** docker-carparts-web
- **Severitat de l'Alerta:** critical
- **Estat Final del Servei:** OPERATIONAL

---

## 2. Símptomes Detectats
L'alerta s'ha disparat automàticament mitjançant la monitorització de logs centralitzats a 'rsyslog-detector' al fitxer `/var/log/remote/10.0.27.223/docker-carparts-web.log`.

Evidència de la línia de log capturada:
`2026-06-16T14:12:34+02:00 web1 docker-carparts-web: [2026-06-16 12:12:34 +0000] [7] [INFO] Worker exiting (pid: 7)`

---

## 3. Diagnòstic de la Infraestructura
Després de rebre l'alerta, s'han executat comandes de diagnòstic no destructives directament sobre el node afectat:

- **Estat del contenidor Docker (via SSH):**
  La sortida de la comanda d'inspecció és: `f365481fdc9d   vm-web-web    "gunicorn --bind 0.0…"   2 weeks ago    Exited (0) 4 hours ago              carparts-web`.
  *Anàlisi:* El contenidor no estava operatiu; es trobava en estat 'Exited (0)' des de feia 4 hores, cosa que confirma que la sortida del worker va derivar en la parada completa del servei i no en un reinici intern gestionat per Gunicorn.

---

## 4. Accions Executades
Descriu de manera cronològica i concisa les accions realitzades pel flux automatitzat de n8n:
1. Recepció de la traça del log crític des del servei de detecció de patrons.
2. Connexió SSH automatitzada al node afectat (10.0.27.223) per verificar l'estat d'execució del contenidor.
3. Execució del comando de recuperació `docker start carparts-web` després de la validació d'estat.
4. Llançament de petició de sondeig HTTP a l'endpoint del servei per validar la disponibilitat real des de l'exterior a través del Proxy.

---

## 5. Resultat Final
A data de `2026-06-16T16:02:59 GMT`, les proves de connectivitat web retornen un codi d'estat `200 OK`. El servei se situa operatiu i accessible, confirmant que la incidència s'ha resolt mitjançant el reinici del contenidor.

---

## 6. Errors Pendents o Riscos
- **Anàlisi de persistència:** En tractar-se d'un error de timeout d'un worker que ha provocat la caiguda del contenidor, hi ha un risc de recursió si l'aplicació pateix pics de càrrega de treball o si existeixen consultes bloquejants o bloquejos mutus (deadlocks) amb el node de base de dades central de la infraestructura.

---

## 7. Recomanacions Futures
- Mantenir el monitoratge estricte d'aquest patró de log. Si la freqüència d'aparició augmenta, es recomana augmentar el valor del paràmetre del temps d'espera (timeout) dels workers dins de la configuració de Gunicorn al desplegament de l'aplicació, o revisar el rendiment de les consultes SQL cap a la base de dades.